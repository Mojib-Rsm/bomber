import { ApiNode } from './types';

// The hardcoded list is now empty. 
// Admins must use the "Import JSON" feature in the Admin Panel to load APIs 
// or add them manually via the UI.

export const INITIAL_API_NODES: ApiNode[] = [];